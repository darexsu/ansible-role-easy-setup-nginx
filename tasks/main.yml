- name: include nginx install
  include_tasks: ./tasks/install/nginx_install_{{ ansible_distribution | lower }}.yml
  when: nginx_install

- name: include nginx settings
  include_tasks: ./tasks/settings/nginx_settings.yml
  when: nginx_settings
